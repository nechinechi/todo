<% res = conn.exec("SELECT ident FROM taskdata WHERE task='params[:taskVal]' AND line='params[:lineVal]'")
   if res
 %>
<%= "There is same Task." %>
<% else %>

   <% res = @conn.exec("INSERT INTO taskdata VALUES(0, 'taskVal', 'lineVal', 'ToDo')")
      if res
   %>
	<%= "Inserting is SUCCESS!" %>
   <% else %>
     	<%= "FAULT" %>
   <% end %>
<% end %>